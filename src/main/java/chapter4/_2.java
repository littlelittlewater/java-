package chapter4;

import org.junit.Test;

/**
 * 实例封闭
 *
 */
public class _2 {
/**
 * 通过一个锁，来保证这个对象的所有访问
 * 被封闭的对象一定不能逸出其所在的作用域
 *
 * 使用锁对象，而不是内置锁有许多优点：
 *  私有锁可以将锁封装起来，避免客户端使用，防止产生活跃度问题
 *  而且使用公有锁，检查是否正常使用，要检查整个程序，而不是单个的类
 *
 *  下面这种情况中，虽然point不是线程安全的，但是Tracker类确实线程安全的
 *
 */
  //TODO 添加演示代码
}














































































































































































































































































































































































